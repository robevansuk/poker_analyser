package com.morphiles.gui;


/**
 * meant to be able to read hands (somehow) AI? Pure Logic?
 */
public class Reader {

    // worth noting when there are 3 high cards its likely someone hit something in a raised pot
    // however in an unraised pot, whilst it's still likely, its less so because
    // no one showed any signs of strength early on.

    // Also, in an unraised pot, its likely someone hit a flop with low/medium cards

}
